import { RoutePlaceholder } from "@/components/shell/route-placeholder";

export default function InternalModerationPage() {
  return (
    <RoutePlaceholder
      routePath="/internal/moderation"
      phase="Phase 2"
      title="Internal moderation route shell"
      description="Reserved internal moderation route inside the approved route-family topology."
    />
  );
}
