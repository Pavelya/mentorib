import { RoutePlaceholder } from "@/components/shell/route-placeholder";

export default function InternalReferenceDataPage() {
  return (
    <RoutePlaceholder
      routePath="/internal/reference-data"
      phase="Phase 2"
      title="Internal reference-data route shell"
      description="Reserved internal route for future governed configuration and reference-data management."
    />
  );
}
